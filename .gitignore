# RubyMine IDE project configuration
.idea

# Plasmo Folder - Contains all the web-browser files generated by Plasmo during runtime
.plasmo

# Plasmo Build Folder - Contains a production-ready version of the web browser extension
build

# Javascript dependencies
node_modules

# TailwindCSS stylesheet - Compiled via JIT during project build (so no need to add it to the repo)
/assets/css/style.css

# Storybook specific files to be ignored
*storybook.log
storybook-static
/.env
